#include<stdio.h>

int main(){
	int y, x, n, i, j, k;
	int w[1000], v[1000], t[1000], h[1000];
	scanf("%d %d %d",&y, &x, &n);
	for(i = 1; i <= n; i++){
		scanf("%d %d %d %d",&w[i], &v[i], &t[i], &h[i])
	}
}
